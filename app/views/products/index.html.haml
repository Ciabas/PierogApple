.container
  %h1 Produkty:
  %table.table.table-striped
    %thead
      %tr
        %th{data_field: :product} Produkt
        %th{data_field: :price} Cena
        %th{data_field: :category} Kategoria
        %th{data_field: 'gear.parrent'} Typ
        %th{data_field: :gear} Model
    -@products.each do |p|
      %tr
        %td
          =p.name
        %td
          =p.price
        %td
          =p.category.name
        %td
          =Gear.find(p.gear.parrent_id).name
        %td
          =p.gear.name
        %td
          =link_to 'wiecej', product_path(p.id)
.container
  %ul
    -@product.errors.full_messages.each do |err|
      %li
        =err
  .row
    .col-md-4.col-md-offset-4
      %h1 Edytuj produkt
  =form_for @product, :html => {multipart: true}, url: admin_product_path(@product) do |f|
    =render 'form', f: f

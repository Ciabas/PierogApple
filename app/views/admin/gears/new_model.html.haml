.row
  .col-md-4.col-md-offset-4.text-center
    %h2 Stwórz nowy model
    = form_for @gear, :html => {multipart: true}, url: admin_gears_new_model_path, html: { class: 'form-inline' } do |f|
      %ul
        -@gear.errors.full_messages.each do |err|
          %li
            =err
      .form-group
        = f.label :name, 'Nazwa', class: 'control-label'
        = f.text_field :name, class: 'form-control' 
      .form-group
        = f.label :parent_id, 'Typ', class: 'control-label'
        = f.collection_select :parent_id, @types, :id, :name, {}, {class: 'form-control'}
        = f.submit "Zapisz", class: 'btn btn-primary' 
